   <h3 class="d-inline-flex bg-light">商品一覧</h3>

   <%= link_to '＋', new_admin_item_path, class:'btn btn-secondary btn-circle' %>

    <table class="table table-hover table-inverse mt-5">
    <thead>
       <tr>
         <th>商品ID</th>
         <th>商品名</th>
         <th>税抜価格</th>
         <th>ジャンル</th>
         <th></th>
         <th>ステータス</th>
       <tr>
    </thead>

    <tbody>
     <% @item.each do |item| %>
      <tr>
        <td><%= item.id %></td>
        <td><%= link_to item.name, admin_item_path(item.id) %></td>
        <td><%= item.price %></td>
        <td><%= item.genre_id %></td>
        <td></td>
        <td><%= item.is_active%></td>
      </tr>
      <% end %>
    </tbody>
</table>
    < pagenate >


